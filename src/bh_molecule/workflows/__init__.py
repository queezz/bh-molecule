"""Batch-oriented workflows (BH fitting, preprocessing, calibration helpers).

Submodules are imported explicitly in application code, e.g.
``from bh_molecule.workflows.preprocessing import prepare_bh_fit_arrays``.
This file marks ``bh_molecule.workflows`` as a regular package so tools such as
mkdocstrings can resolve ``bh_molecule.workflows.*`` reliably.
"""
